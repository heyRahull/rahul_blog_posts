---
title: "React Context Api"
slug: react-context-api

---

React Context Api is one of the most important skill to know in reactjs, so let's discuss about it in this blog. Also there are many times when people get confused between whether to use Context Api or Redux. So I would suggest if you dont know any then start with Context Api and then move towards Redux afterwards. Context Api is much simpler to learn comparatively and also learning this will help you later when you will learn Redux. The learning curve in Redux is a bit high therefore it is much better to start with Context Api and then move towards Redux. 

# Let's get started with Context Api.
React Context API was introduced in React 16. Many years ago React has offered an experimental API for context. Although it was a powerful tool, its use was discouraged because of inherent problems in the API. Also, the developers were not much confident to use it at that time as it was in experimental stage. React Context API was introduced around the time when functional components were introduced in React.

In Version 16.3, a new Context API that is more efficient and supports both static checking and deep updates.